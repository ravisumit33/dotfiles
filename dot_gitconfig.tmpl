[user]
    name = Sumit Kumar
    email = ravi.sumit33@gmail.com

{{ $workPath := printf "%s/%s" .chezmoi.homeDir .work.dirname }}

[includeIf "gitdir:{{$workPath}}/"]
    path = ~/.gitconfig-work

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    dark = true
    side-by-side = true
    line-numbers = true

[diff]
    colorMoved = default

[merge]
    conflictstyle = diff3
